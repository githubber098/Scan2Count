[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    unit: Unit tests — fully mocked, no network calls
    integration: Route tests — dependency overrides + mocked services
    e2e: End-to-end tests against real sandbox (skipped by default; set SANDBOX_E2E=1 to run)
